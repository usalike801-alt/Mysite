import { AdminPanel } from '@/components/admin-panel'

export const metadata = {
  title: 'Admin — Ascend',
  description: 'Manage pricing, orders, and API configuration.',
}

export default function AdminPage() {
  return <AdminPanel />
}
